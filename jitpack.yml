jdk:
  - openjdk17

before_install:
  - echo "Using Java 17 for JitPack build"

build:
  script:
    - ./gradlew clean -Pgroup=com.github.lamlong97 -Pversion=1.0.14 -xtest -xlint assemble publishToMavenLocal
