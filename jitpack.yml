jdk:
  - openjdk17

before_install:
  - echo "Using Java 17 for JitPack build"

install:
  - ./gradlew clean :app:assembleRelease :app:publishToMavenLocal -Pgroup=com.github.lamlong97 -Pversion=1.0.28 -xlint -xtest
